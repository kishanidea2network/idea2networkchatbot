services:
  - type: web
    name: idea2network-chatbot
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: GROQ_API_KEY
        sync: false   # Set this manually in Render dashboard
      - key: PORT
        value: 3000
